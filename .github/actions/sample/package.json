{
  "name": "ownActions",
  "version": "1.0.0",
  "dependencies": {
    "@actions/core": "^1.6.0"
  },
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build index.js -o dist --source-map"
  }
}